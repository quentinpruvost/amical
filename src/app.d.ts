// src/app.d.ts
declare global {
	namespace App {
		// interface Error {}
		// Supprimez ou commentez ces lignes
		// interface Locals {
		// 	user?: {
		// 		isAuthenticated: boolean;
		// 	}
		// }
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};